import { createContext } from 'react'; // Traemos la herramienta para crear el contexto global

// Creamos y exportamos la variable que servirá de canal para el tema claro/oscuro
export const ThemeContext = createContext();